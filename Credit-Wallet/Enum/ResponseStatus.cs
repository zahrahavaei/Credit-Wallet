namespace Credit_Wallet.Enum
{
    public enum ResponseStatus
    {
        Success,
        InvalidRequest,
        IntegrityFailed,
        PasswordVerificationFailed,
        NotFound,

        Error
    }
}
